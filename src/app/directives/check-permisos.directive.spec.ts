import { CheckPermisosDirective } from './check-permisos.directive';

describe('CheckPermisosDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckPermisosDirective();
    expect(directive).toBeTruthy();
  });
});
